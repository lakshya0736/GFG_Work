class Solution {
    public Node findLCA(Node root, Node p, Node q) {
        if(root.data>q.data && root.data>p.data) return findLCA(root.left,p,q);
        else if(root.data<p.data && root.data<q.data) return findLCA(root.right,p,q);
        else return root;
    }        
}
