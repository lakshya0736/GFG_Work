class Solution {
    public Stack<Integer> insertAtBottom(Stack<Integer> st, int ele) {
        if(st.size()==0){
            st.push(ele);
            return st;
        }
        int top = st.pop();
        insertAtBottom(st,ele);
        st.push(top);
        return st;
    }
}
