class Solution {
    public static void reverseStack(Stack<Integer> st) {
        int[] arr = new int[st.size()];
        int i=0;
        while(st.size()>0){
            arr[i] = st.pop();
            i++;
        }
        for(i=0;i<arr.length;i++){
            st.push(arr[i]);
        }
    }
}
